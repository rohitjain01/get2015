Create database EmployeeDetail;
use EmployeeDetail;
CREATE TABLE Employee(  
     id integer auto_increment,
     name VARCHAR (50),
     email VARCHAR (50),
     address VARCHAR (50),
     primary key (id) );