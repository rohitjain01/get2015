Module         		: WEB
Assignment for Session	: JSP & Servlets Session 1
Author			: Rohit

----------------------------------------------------------------
1. Write a web application that:
   Contains a user registration information form that requests a servlet.
   Contains a servlet that accepts the request sent by client, validates the form data and displays the validation result on the browser.

2. Write a web application that:
   Contains a user registration information form that requests a servlet.
   Contains a servlet that accepts the request sent by client, 
   validates the form data and dispatches the validation result to the other servlet and that servlet displays the validation result.


